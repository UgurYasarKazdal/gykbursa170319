<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gyk.bursa162">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"></uses-permission>
    <uses-permission android:name="android.permission.RECORD_AUDIO"></uses-permission>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo_162_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_162_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OgrenciActivity" />
        <activity android:name=".DenemeActivity" />
        <activity android:name=".KonukActivity" />
        <activity android:name=".SmsActivity" />
        <activity android:name=".WebActivity" />
        <activity android:name=".CameraActivity"/>
        <activity android:name=".CallActivity"/>
        <activity android:name=".MapActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".VoiceActivity">

        </activity>
    </application>

</manifest>